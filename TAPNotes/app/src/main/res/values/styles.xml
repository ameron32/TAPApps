<resources>

  <style name="AppBaseTheme" parent="Theme.AppCompat.Light"></style>

  <!-- Application theme. -->
  <style name="AppTheme" parent="AppBaseTheme">
    <!-- All customizations that are NOT specific to a particular API-level can go here. -->
  </style>

  <!--<style name="CustomTheme" parent="Theme.AppCompat.Light.NoActionBar">-->
  <style name="CustomTAPTheme" parent="MaterialDrawerTheme.Light">

    <!-- Customize your theme here. -->
    <item name="colorPrimary">@color/myPrimaryColor</item>
    <item name="colorPrimaryDark">@color/myPrimaryDarkColor</item>
    <item name="colorAccent">@color/myAccentColor</item>
    <item name="android:textColorPrimary">@color/myTextPrimaryColor</item>
    <item name="drawerArrowStyle">@style/DrawerArrowStyle</item>
    <item name="android:windowBackground">@color/myWindowBackground</item>

    <!--<item name="snack_bar_text_color">@color/myTextPrimaryHintColor</item>-->
    <!--<item name="snack_bar_duration">5000</item>-->

    <item name="windowNoTitle">true</item>
    <!-- For 2.x version -->
    <item name="windowActionBar">false</item>
  </style>

  <style name="Theme.AppCompat.Light.NoActionBar" parent="@style/Theme.AppCompat.Light">
    <item name="windowNoTitle">true</item>
    <!-- For 2.x version -->
    <item name="windowActionBar">false</item>
  </style>

  <style name="ToolBarStyle" parent="">
    <item name="popupTheme">@style/ThemeOverlay.AppCompat.Light</item>
    <item name="theme">@style/ThemeOverlay.AppCompat.Dark.ActionBar</item>
  </style>

  <style name="DrawerArrowStyle" parent="Widget.AppCompat.DrawerArrowToggle">
    <item name="spinBars">true</item>
    <item name="color">@android:color/white</item>
  </style>

  <style name="Widget.Philm.Toolbar" parent="Widget.AppCompat.Toolbar">
    <item name="android:background">?attr/colorPrimary</item>
    <item name="popupTheme">@style/ThemeOverlay.AppCompat.Light</item>
    <item name="theme">@style/ThemeOverlay.AppCompat.Dark.ActionBar</item>
    <item name="contentInsetStart">24dp</item>
  </style>

  <style name="Widget.Philm.Toolbar.Transparent">
    <item name="android:background">@null</item>
  </style>

    <style name="TextAppearance.TAPNotes.FloatLabel" parent="android:TextAppearance.Small">
        <item name="android:textColor">@color/float_label</item>
        <item name="android:textSize">12sp</item>
    </style>

</resources>
